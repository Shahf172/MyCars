@model IEnumerable<MyCars.Models.Automobile>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AutomobilesCompany.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ModelDetail.ModelName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AutomobilesCompany.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ModelDetail.ModelName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AutomobileID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AutomobileID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AutomobileID })
        </td>
    </tr>
}

</table>
